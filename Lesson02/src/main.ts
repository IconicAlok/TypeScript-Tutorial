let myName: string = "Alok";
let meaningOfLife: number ;
let isLoading: boolean;
let album: number | string;

myName = 'John';
meaningOfLife = 42;
isLoading = true;
album = 'Van Halen';

const sum = (a : number, b : string)=>{
    return a + b;
}

let postID : string | number
let isActive : number | boolean

let re : RegExp = /\w+/g